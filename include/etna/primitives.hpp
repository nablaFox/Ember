#include "mesh.hpp"

namespace etna::utils {

inline void createTexturedSphere(std::vector<Vertex>& vertices,
								 std::vector<Index>& indices) {}

inline void createTexturedCube(std::vector<Vertex>& vertices,
							   std::vector<Index>& indices) {}

constexpr uint32_t SPHERE_PRIMITIVE_INDICES_COUNT = 0;
constexpr uint32_t CUBE_PRIMITIVE_INDICES_COUNT = 0;

}  // namespace etna::utils
